---
export interface Props {
  color: string
}

const { color } = Astro.props
---

<div class="flex flex-col items-center justify-center">
  <div class={`h-4 w-4 bg-[#${color}]`}></div>
  <span class="text-base">
    <slot />
  </span>
</div>
